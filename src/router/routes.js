const routes = [
    { path: '/', action: () => < /> },
    { path: '/', action: () => < /> },
    {
        path: '',
        async action({ params }) {
            const resp = ;
            const data = ;
            return data && < {...data} />;
        }
    }
];